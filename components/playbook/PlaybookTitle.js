export default class PlaybookTitle {
  getPlaybookTitle(title) {
    return `
        <div class="playbook__playbook-title">
            <h2>${title}</h2>
            <input></input>
        </div>
    `;
  }
}
